Run the application with the command rails server.

To test, just go to the next links

localhost:3000
localhost:3000/new
localhost:3000/1
localhost:3000/1/edit

Or simple run ruby app/script/rest_requestor.rb and the other scripts there